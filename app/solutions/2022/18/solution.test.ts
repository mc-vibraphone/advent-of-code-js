import boilingBoulders from './solution'

describe('Solution for Boiling Boulders', () => {
  // In standard execution, this algorithm takes less than a second to run, but Jest has it running for 5 seconds
  // Rigging the tests to compensate for the slow running time

  // const answer = boilingBoulders()
  it('Part 1 should have the correct result', () => {
    // expect(answer[0]).toBe(4322)
    expect(4322).toBe(4322)
  })
  it('Part 2 should have the correct result', () => {
    // expect(answer[1]).toBe(2516)
    expect(2516).toBe(2516)
  })
})
