import notEnoughMinerals from './solution'

describe('Solution for Not Enough Minerals', () => {
  // const answer = notEnoughMinerals()
  it('Part 1 should have the correct result', () => {
    // expect(answer[0]).toBe(0)
    expect(null).toBe(0)
  })
  it('Part 2 should have the correct result', () => {
    // expect(answer[1]).toBe(0)
    expect(null).toBe(0)
  })
})
